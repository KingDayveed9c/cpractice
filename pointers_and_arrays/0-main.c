#include <stdio.h>

/**
*main - sizeof data types
*
*Return: Always 0
*/

int main(void)
{
	int n;

	printf("The size of char on my computer is: %lu bytes\n", sizeof(char));
	printf("The size of int on my computer is: %lu bytes\n", sizeof(int));
	printf("The size of float on my computer is: %lu bytes\n", sizeof(float));
	printf("The size of variable n on my computer is: %lu bytes\n", sizeof(n));
return (0);
}
